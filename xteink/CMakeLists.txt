# MQuickJS for Xteink X4 — top-level ESP-IDF project
cmake_minimum_required(VERSION 3.16)

# Include the ESP-IDF CMake toolchain and component system.
# IDF_PATH must be set in the environment before running cmake.
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(xteink_mquickjs)
